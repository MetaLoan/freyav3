import { s, fs, vs, ms, wp, hp, screenWidth, isTablet } from '../utils/responsive';

/**
 * Freya V3 布局标准规范
 * 
 * 所有页面级和组件级的尺寸标准。
 * 全部经过响应式缩放处理，确保不同设备视觉比例一致。
 * 
 * 设计基准：375dp (iPhone 标准)
 * 
 * 使用方式：
 * ```tsx
 * import { layout } from '@/config/layout';
 * 
 * <YStack paddingHorizontal={layout.page.paddingH}>
 *   <Card height={layout.card.heightLg} borderRadius={layout.card.radius}>
 *     ...
 *   </Card>
 * </YStack>
 * ```
 */

export const layout = {
  // ============================================================
  // 页面级布局
  // ============================================================
  page: {
    /** 页面左右内边距 */
    paddingH: s(20),
    /** 页面顶部内边距（不含安全区域，安全区域由 SafeAreaWrapper 处理） */
    paddingTop: s(16),
    /** 页面底部内边距（不含底部导航高度） */
    paddingBottom: s(24),
    /** 内容最大宽度（平板/Web 端限制，防止内容拉伸过宽） */
    maxWidth: isTablet ? 480 : screenWidth,
    /** 区块之间的垂直间距（如"配色"区块和"字体"区块之间） */
    sectionGap: s(28),
    /** 同一区块内元素间距 */
    itemGap: s(12),
  },

  // ============================================================
  // 头部导航
  // ============================================================
  header: {
    /** 头部总高度（不含安全区域） */
    height: s(56),
    /** 头部左右内边距 */
    paddingH: s(20),
    /** 头部标题字号 */
    titleSize: fs(18),
    /** 头部图标尺寸 */
    iconSize: ms(24, 0.5),
    /** 头部图标点击区域 */
    iconHitSlop: s(44),
    /** 返回按钮尺寸 */
    backButtonSize: s(36),
  },

  // ============================================================
  // 底部导航栏
  // ============================================================
  bottomNav: {
    /** 导航栏总高度 */
    height: s(72),
    /** 导航栏内边距 */
    paddingH: s(16),
    /** 导航栏底部安全间距 */
    paddingBottom: s(8),
    /** 图标容器尺寸（未激活） */
    iconContainer: s(44),
    /** 图标容器尺寸（激活态，稍大） */
    iconContainerActive: s(52),
    /** 图标大小 */
    iconSize: ms(22, 0.5),
    /** 标签字号 */
    labelSize: fs(10),
    /** 图标与标签间距 */
    iconLabelGap: s(4),
  },

  // ============================================================
  // 卡片组件
  // ============================================================
  card: {
    /** 卡片圆角 */
    radius: s(20),
    /** 卡片内边距 - 紧凑 */
    paddingSm: s(12),
    /** 卡片内边距 - 标准 */
    padding: s(16),
    /** 卡片内边距 - 宽松 */
    paddingLg: s(20),
    /** 卡片边框宽度 */
    borderWidth: 1,
    /** 卡片之间的间距 */
    gap: s(12),
    /** 小卡片高度（如功能入口卡片） */
    heightSm: vs(80),
    /** 中卡片高度（如信息展示卡片） */
    heightMd: vs(120),
    /** 大卡片高度（如 CTA / Premium 卡片） */
    heightLg: vs(180),
  },

  // ============================================================
  // 按钮组件
  // ============================================================
  button: {
    /** 小按钮高度 */
    heightSm: s(32),
    /** 标准按钮高度 */
    height: s(44),
    /** 大按钮高度 */
    heightLg: s(52),
    /** 按钮圆角 */
    radius: s(16),
    /** 按钮圆角 - 圆形（胶囊形） */
    radiusFull: 9999,
    /** 按钮水平内边距 */
    paddingH: s(20),
    /** 小按钮字号 */
    fontSizeSm: fs(12),
    /** 标准按钮字号 */
    fontSize: fs(14),
    /** 大按钮字号 */
    fontSizeLg: fs(16),
    /** 按钮图标大小 */
    iconSize: ms(18, 0.5),
    /** 按钮图标与文字间距 */
    iconGap: s(8),
  },

  // ============================================================
  // 输入框组件
  // ============================================================
  input: {
    /** 输入框高度 */
    height: s(48),
    /** 输入框圆角 */
    radius: s(16),
    /** 输入框左右内边距 */
    paddingH: s(16),
    /** 输入框字号 */
    fontSize: fs(14),
    /** 聊天输入框高度（底部浮动输入栏） */
    chatBarHeight: s(52),
    /** 聊天输入栏圆角 */
    chatBarRadius: 9999,
    /** 聊天发送按钮尺寸 */
    chatSendSize: s(40),
  },

  // ============================================================
  // 头像组件
  // ============================================================
  avatar: {
    /** 小头像 */
    sizeSm: s(32),
    /** 标准头像 */
    size: s(44),
    /** 中头像 */
    sizeMd: s(56),
    /** 大头像（个人中心） */
    sizeLg: s(80),
    /** 特大头像（占星师卡片） */
    sizeXl: s(96),
    /** 金色边框宽度 */
    borderWidth: s(2),
    /** 在线状态点尺寸 */
    statusDotSize: s(10),
  },

  // ============================================================
  // 消息气泡（Chat）
  // ============================================================
  bubble: {
    /** 气泡最大宽度比例 */
    maxWidthPercent: 85,
    /** 气泡圆角 */
    radius: s(18),
    /** 气泡内边距 */
    padding: s(12),
    /** 气泡水平内边距 */
    paddingH: s(14),
    /** 气泡字号 */
    fontSize: fs(14),
    /** 时间戳字号 */
    timestampSize: fs(10),
    /** 气泡之间的垂直间距 */
    gap: s(8),
    /** AI 头像尺寸 */
    avatarSize: s(32),
  },

  // ============================================================
  // 列表/行组件
  // ============================================================
  listItem: {
    /** 列表项高度 */
    height: s(56),
    /** 列表项左右内边距 */
    paddingH: s(16),
    /** 列表项垂直内边距 */
    paddingV: s(12),
    /** 列表项图标容器尺寸 */
    iconContainer: s(40),
    /** 列表项图标容器圆角 */
    iconRadius: s(12),
    /** 列表项标题字号 */
    titleSize: fs(15),
    /** 列表项描述字号 */
    descSize: fs(12),
    /** 列表项右侧箭头大小 */
    chevronSize: ms(20, 0.5),
  },

  // ============================================================
  // 弹窗/模态框
  // ============================================================
  modal: {
    /** 弹窗宽度百分比 */
    widthPercent: isTablet ? 70 : 90,
    /** 弹窗最大宽度 */
    maxWidth: 420,
    /** 弹窗圆角 */
    radius: s(24),
    /** 弹窗内边距 */
    padding: s(24),
    /** 弹窗标题字号 */
    titleSize: fs(20),
    /** 遮罩层透明度 */
    overlayOpacity: 0.7,
  },

  // ============================================================
  // 分割线
  // ============================================================
  separator: {
    /** 分割线上方间距 */
    marginTop: s(8),
    /** 分割线下方间距 */
    marginBottom: s(8),
    /** 分割线厚度 */
    thickness: 1,
  },

  // ============================================================
  // 徽标/标签
  // ============================================================
  badge: {
    /** 小徽标（通知点） */
    dotSize: s(8),
    /** 标准徽标尺寸 */
    size: s(18),
    /** 徽标字号 */
    fontSize: fs(10),
    /** 标签（Tag）高度 */
    tagHeight: s(28),
    /** 标签圆角 */
    tagRadius: s(14),
    /** 标签水平内边距 */
    tagPaddingH: s(12),
    /** 标签字号 */
    tagFontSize: fs(12),
  },

  // ============================================================
  // 工具类
  // ============================================================

  /** 获取页面内容区域样式（统一左右边距 + 最大宽度） */
  getPageStyle: () => ({
    paddingHorizontal: s(20),
    maxWidth: isTablet ? 480 : undefined,
    alignSelf: isTablet ? 'center' as const : undefined,
    width: isTablet ? '100%' as const : undefined,
  }),

  /** 获取卡片标准样式 */
  getCardStyle: () => ({
    borderRadius: s(20),
    padding: s(16),
    borderWidth: 1,
  }),
} as const;
